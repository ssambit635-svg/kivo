/* ogl — minimal vendored subset (v1.0.11, MIT License, © oframe).
 * Only the classes the landing "weave" section needs, so the page
 * ships one small ESM graph instead of the whole library.
 * Upstream: https://github.com/oframe/ogl */
export { Renderer } from './core/Renderer.js';
export { Program } from './core/Program.js';
export { Mesh } from './core/Mesh.js';
export { Triangle } from './extras/Triangle.js';
