<?xml version="1.0" encoding="utf-8"?>
<!--
  Framework Material theme on purpose: the shell has zero dependencies, so no
  AppCompat / Material Components library is pulled in. minSdk is 24, which is
  above the API 21 floor for Theme.Material.
-->
<resources>
    <style name="Theme.Kivo" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/brand</item>
        <item name="android:colorPrimaryDark">@color/brand_dark</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/brand_dark</item>
        <item name="android:navigationBarColor">@color/brand_dark</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:textColorPrimary">@color/ink</item>
        <item name="android:textColorSecondary">@color/ink_dim</item>
    </style>
</resources>
